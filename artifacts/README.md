# Artifact Layout

Use this folder as a staging area before creating the final ZIP.

Recommended contents:
- `model/` -> final checkpoint or link manifest
- `report/` -> report PDF and time/resource log
- `figures/` -> final qualitative outputs and confusion matrices
- `metrics/` -> validation/test JSON metrics and tables
